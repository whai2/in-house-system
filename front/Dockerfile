FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci

# 소스 코드 복사
COPY . .

# 빌드
RUN npm run build

# 포트 노출
EXPOSE 3000

# preview 서버 실행
CMD ["npm", "run", "preview", "--", "--host", "0.0.0.0", "--port", "3000"]
